<div class="container">
  <h1>Restaurants::Reservations#index</h1>
  <p>Find me in app/views/restaurants/reservations/index.html.erb</p>

  <%= link_to "Change Password", edit_restaurant_registration_path, class: "btn btn-primary" %>
  <%= link_to "Edit Profile", edit_restaurants_profile_path(current_restaurant), class: "btn btn-warning" %>

  <hr>

  <div class="row">
    <div class="col-xs-12  col-sm-6 col-md-4">
      <div class="card">
        <p>Pending</p>
        <% @reservations.each do |reservation| %>
          <% if reservation.status == "pending" %>
            <%= reservation.time %>
            <%= link_to reservation, restaurants_reservation_path(reservation) %>
          <% end %>
        <% end %>
      </div>

      <hr>

      <div class="card">
        <p>Confirmed</p>
        <% @reservations.each do |reservation| %>
          <% if reservation.status == "confirmed" %>
            <%= reservation.time %>
            <%= link_to reservation, restaurants_reservation_path(reservation) %>
          <% end %>
        <% end %>
      </div>
    </div>
  </div>

  <br>
</div>


